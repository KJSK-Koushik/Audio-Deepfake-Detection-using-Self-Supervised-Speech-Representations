# Data Directory

Phase 1 will create these local directories:

- `raw/asvspoof2019_la/`: downloaded ASVspoof 2019 LA files
- `metadata/`: parsed train, development, and evaluation CSV files
- `processed/`: generated preprocessing indexes or cached features

The dataset license and redistribution rules apply. Audio and generated data are ignored by Git and must not be committed.
