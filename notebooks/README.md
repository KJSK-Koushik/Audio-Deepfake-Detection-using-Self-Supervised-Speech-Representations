# Notebooks

Optional exploratory notebooks belong here. Reusable preprocessing, model, and evaluation logic must live under `src/` so it can be tested and reused by training and the demo application.
