# Model Artifacts

Trained checkpoints, serialized baseline models, and exported inference files are stored here locally. They are ignored by Git because they can be large and must be reproducible from configuration and training code.
